passdb {
  driver = sql
  args = /etc/dovecot/dovecot-sql.conf.ext
}
userdb {
  driver = static
  args = uid=<%= node['mailserver']['vmail_user'] %> gid=<%= node['mailserver']['vmail_group'] %> home=<%= node['mailserver']['storage_path'] %>/%d/%n
}
